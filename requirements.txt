# ---- Web Framework & Server ----
# FastAPI is the main framework, and Uvicorn runs the server.
fastapi
uvicorn[standard]

# ---- OCR and Computer Vision ----
# PaddleOCR is the OCR engine, PaddlePaddle is its deep learning backend,
# and OpenCV is used for image preprocessing.
paddleocr
paddlepaddle
opencv-python

# ---- Text Processing ----
# TheFuzz is used for fuzzy string matching to correct typos.
# [speedup] automatically includes python-levenshtein for better performance.
thefuzz[speedup]

# ---- API File Handling ----
# Required by FastAPI to handle image file uploads.
python-multipart
